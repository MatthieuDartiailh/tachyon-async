[package]
name = "tachyon-tokio"
version = "0.1.0"
edition = "2021"

[dependencies]
tachyon-async-core = { path = "../tachyon-async-core" }
tachyon-ipc = "0.5.1"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync", "time"] }
